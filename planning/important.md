# Important

**Project overview (done / next / repo notes):** see **`SUMMARY.md`**.

**Future runs:** use Docker Compose so dependencies (tau-bench, uv, etc.) match the project. Prefer:

```bash
docker compose run autoeval python <script>.py
```

Examples: `prepare.py`, `benchmark.py`, `gating.py`, `record.py` — see **`README.md`** for other examples.
