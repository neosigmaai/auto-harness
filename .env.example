# LLM API keys (set whichever your agent_model needs)
OPENAI_API_KEY=
GEMINI_API_KEY=

# Terminal-Bench sandbox provider (set one)
E2B_API_KEY=
DAYTONA_API_KEY=

# Tau-bench data directory (only needed for benchmark: "tau-bench")
# Defaults to ./tau2_data in the repo root if not set.
# TAU2_DATA_DIR=
