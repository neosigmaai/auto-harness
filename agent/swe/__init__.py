"""
SWE-Bench harness integration (predictions JSONL, Docker eval, diagnostics).

Import from submodules explicitly:

- ``from agent.swe.runner import SWEBenchRunner``
- ``from agent.swe.diag import finalize_swe_benchmark_artifacts``
- ``from agent.swe.log_collapse import collapse_run_instance_log_text``
"""
