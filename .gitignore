__pycache__/
*.pyc
*.pyo
.venv/
.env
.DS_Store
*.log

# agent working directory (runtime state, not committed)
workspace/

# local coding agent config
.claude/

# local experiment config (created by prepare.py from template)
experiment_config.yaml

# build/data artifacts
auto_harness.egg-info/

# tau2 data (auto-cloned by prepare.py, not committed)
tau2_data/

# WebArena source clone (auto-cloned by prepare.py, not committed)
webarena_repo/
# Auto-generated split (keep the curated candidate file, not the generated split)
webarena_data/task_split.json

# Terminal-Bench split generated by prepare.py from a baseline run
tbench_data/

# uv lockfile (generated locally; not pinned in VCS)
uv.lock

# local experiment_config backups
experiment_config.yaml.bak
